<%= form_for(@squeek) do |f| %>
  <% if @squeek.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@squeek.errors.count, "error") %> prohibited this squeek from being saved:</h2>

      <ul>
      <% @squeek.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br>
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
